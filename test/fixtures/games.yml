DEFAULTS: &DEFAULTS
  round: 1

resource_purchase:
  <<: *DEFAULTS
  token: <%= SecureRandom.hex(3) %>

auction_creator:
  <<: *DEFAULTS
  token: <%= SecureRandom.hex(3) %>

only_bidder:
  <<: *DEFAULTS
  token: <%= SecureRandom.hex(3) %>

multiple_bidders:
  <<: *DEFAULTS
  token: <%= SecureRandom.hex(3) %>

building:
  <<: *DEFAULTS
  token: <%= SecureRandom.hex(3) %>

cities_power_up:
  <<: *DEFAULTS
  token: <%= SecureRandom.hex(3) %>

resource_replenishment:
  <<: *DEFAULTS
  token: <%= SecureRandom.hex(3) %>
  step: 2

claim_auction:
  <<: *DEFAULTS
  token: <%= SecureRandom.hex(3) %>
  phase_player_ids:
    <% 2.times do |idx| %>
    - <%= ActiveRecord::FixtureSet.identify("claim_auction_#{idx}") %>
    <% end %>

claim_auction_replace:
  <<: *DEFAULTS
  token: <%= SecureRandom.hex(3) %>
  phase_player_ids:
    <% 2.times do |idx| %>
    - <%= ActiveRecord::FixtureSet.identify("claim_auction_replace_#{idx}") %>
    <% end %>

card_setup:
  <<: *DEFAULTS
  token: <%= SecureRandom.hex(3) %>

new_turn_order:
  <<: *DEFAULTS
  token: <%= SecureRandom.hex(3) %>

skip_auction:
  <<: *DEFAULTS
  token: <%= SecureRandom.hex(3) %>
  phase_player_ids:
    <% 3.times do |idx| %>
    - <%= ActiveRecord::FixtureSet.identify("skip_auction_#{idx}") %>
    <% end %>

resource_purchase_pass:
  <<: *DEFAULTS
  token: <%= SecureRandom.hex(3) %>
  phase_player_ids:
    - <%= ActiveRecord::FixtureSet.identify(:resource_purchase_pass) %>
