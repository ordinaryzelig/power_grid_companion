resource_purchase:
  token: <%= SecureRandom.hex(3) %>

auction_creator:
  token: <%= SecureRandom.hex(3) %>

only_bidder:
  token: <%= SecureRandom.hex(3) %>

multiple_bidders:
  token: <%= SecureRandom.hex(3) %>

building:
  token: <%= SecureRandom.hex(3) %>

cities_power_up:
  token: <%= SecureRandom.hex(3) %>

resource_replenishment:
  token: <%= SecureRandom.hex(3) %>
  step: 2

claim_auction:
  token: <%= SecureRandom.hex(3) %>

claim_auction_replace:
  token: <%= SecureRandom.hex(3) %>
