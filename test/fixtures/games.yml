resource_purchase:
  token: <%= SecureRandom.hex(3) %>

auction_creator:
  token: <%= SecureRandom.hex(3) %>

only_bidder:
  token: <%= SecureRandom.hex(3) %>

multiple_bidders:
  token: <%= SecureRandom.hex(3) %>
