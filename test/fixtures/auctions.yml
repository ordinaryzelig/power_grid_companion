multiple_bidders:
  game: $LABEL
  card: $LABEL
  player: multiple_bidders_0
  bidder_ids:
    <% [2, 0, 1].each do |idx| %>
    - <%= ActiveRecord::FixtureSet.identify("multiple_bidders_#{idx}") %>
    <% end %>
  price: 3

claim_auction:
  game: $LABEL
  card: $LABEL
  player: $LABEL
  bidder_ids:
    - <%= ActiveRecord::FixtureSet.identify(:claim_auction) %>
  price: 3

claim_auction_replace:
  game: $LABEL
  card: claim_auction_replace_6
  player: $LABEL
  bidder_ids:
    - <%= ActiveRecord::FixtureSet.identify(:claim_auction_replace) %>
  price: 6
