%h3 New turn order

%table.table
  %thead
    %th Player
    %th.text-center Cities
    %th.text-center High card
  %tbody
    - @new_turn_order_players.each do |player|
      %tr.player{:data => {:color => player.color}}
        %th
          .house= house_icon
          = player.name
        %td.text-center= player.cities
        %td.text-center= player.cards.map(&:number).max

- if current_player == @controlling_player
  = button_to 'Continue', turn_orders_url, :class => 'btn btn-primary btn-block mt-3'
- else
  Waiting for #{@controlling_player.name} to accept.
